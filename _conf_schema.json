{
    "github_token": {
        "description": "GitHub Personal Access Token",
        "type": "string",
        "default": "",
        "hint": "用于访问私有仓库和组织项目，格式ghp_xxxxxxxxxxxx，需具有repo、read:org、project 权限"
    },
    "poll_interval": {
        "description": "轮询间隔（秒）",
        "type": "int",
        "default": 1800,
        "hint": "每隔多少秒检查一次动态，默认1800秒（30分钟），建议不低于600秒"
    },
    "max_entries": {
        "description": "每次最多推送条目数",
        "type": "int",
        "default": 5,
        "hint": "单次推送每个订阅项最多显示多少条新动态，设为0则不限制"
    },
    "timezone": {
        "description": "时区",
        "type": "string",
        "default": "Asia/Shanghai",
        "hint": "用于将 GitHub 动态的时间转换为本地时间"
    },
    "whitelist": {
        "description": "白名单用户",
        "type": "list",
        "default": [],
        "hint": "只有白名单用户或管理员可以使用订阅管理指令。留空则所有用户均可使用"
    }
}